from setuptools import setup, find_packages

setup(name='qp-wgan',
      version='0.1',
      description='(q,p)-Wasserstein GANs: Comparing Ground Metrics for Wasserstein GANs',
      packages=find_packages())
